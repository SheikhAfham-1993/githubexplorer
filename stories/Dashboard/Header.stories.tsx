import type { Meta } from '@storybook/react'

import Header from '../../components/Header'

export default {
  title: 'Main/Header',
  component: Header,
} as Meta

export const SimpleHeader = {}
